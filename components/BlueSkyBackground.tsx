"use client";

import React from "react";

const BlueSkyBackground = () => {
  return (
    <div className="fixed inset-0 w-full h-full -z-10 overflow-hidden bg-gradient-to-br from-blue-400 to-blue-200"></div>
  );
};

export default BlueSkyBackground;
